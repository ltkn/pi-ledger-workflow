{
  "name": "pi-plan-build",
  "version": "1.0.0",
  "description": "Plan \u2192 spec \u2192 build \u2192 review for the Pi coding agent: plan together, build each feature in a fresh session from a self-contained spec, task by task behind checks the harness runs, then an independent review.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "coding-agent",
    "spec",
    "plan",
    "review",
    "workflow"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ltkn/pi-plan-build.git"
  },
  "homepage": "https://github.com/ltkn/pi-plan-build#readme",
  "files": [
    "extensions",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "pi": {
    "extensions": [
      "./extensions/pb/index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "tsx --test test/pb.test.mts",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.87.1",
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
